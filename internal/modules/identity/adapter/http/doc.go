// Package http implements the identity module's chi handlers and htmx template
// rendering.
//
// Allowed imports: own app, own domain, infra libs (chi, html/template).
// Forbidden imports: other modules' internals (domain, app, adapter).
package http
