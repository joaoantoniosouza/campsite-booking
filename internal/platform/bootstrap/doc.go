// Package bootstrap is the composition root: it builds platform
// dependencies, constructs each module, wires cross-module ports, and mounts
// module HTTP routes onto the chi router.
package bootstrap
